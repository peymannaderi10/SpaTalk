# onboarding-gaps: a tenant from the basics, the wizard's basics path, forwarding instructions, the first-run checklist
Status: done with deviations
Commit: bf4cfb9 (A, runtime: starter bundle, `POST /internal/tenants/from-basics`, `spatalk tenant new`, contract and client regenerated); 7f6e375 (B, portal: the wizard's "Start from the basics"); 4d8c076 (C, portal: "Forward your line" on the Numbers page); c89cfa4 (D, portal: the "Getting set up" card on the overview)
Tests: runtime `uv run pytest -q -p no:cacheprovider tests/test_tenant_starter.py` -> 16/16 (all 16 seen failing first on `ModuleNotFoundError: spatalk.tenants.starter`; then 15/16 with `timezone` not in the contract's required list, fixed by removing its default from the model); `tests/test_renderer.py tests/test_internal_api.py tests/test_tenant_bundle.py tests/test_contract_snapshot.py tests/test_tenant_starter.py tests/test_qa_gate_a.py` -> 162/162; full suite `uv run pytest -q` -> 1297 passed, 2 skipped (the two skips are the live-key tests, `skipif` on the key) in 6 min 29 s; `uv run ruff check spatalk tests scenarios` -> clean. Portal: `npx vitest run src/admin/basics.test.ts src/admin/bundle.test.ts` -> 20/20 (11 new in `basics.test.ts`, seen failing on "Failed to resolve import ./basics"); `npx vitest run -c vitest.server.config.ts src/admin/operations.server.test.ts` -> 7/7 (6 seen failing on `createTenantFromBasics is not a function`; the seventh, the bundle path's taken-address text, passed from the first run and is the baseline the shared tail had to keep); `npx vitest run src/client/settings/NumbersTab.test.tsx src/client/firstRun.test.ts src/client/FirstRunCard.test.tsx` -> 17/17 (4 in `NumbersTab.test.tsx` seen failing 4/4 on the missing card, 9 in `firstRun.test.ts` and 4 in `FirstRunCard.test.tsx` seen failing on "Failed to resolve import ./firstRun"); full client suite `npx vitest run` -> 294/294 (30 files); server suite `npm run test:unit` -> 123/123 (8 files); `npx tsc --noEmit -p tsconfig.src.json` -> exit 0 after each portal commit, once `wasp compile` had regenerated the SDK (see deviations). Playwright not run (needs a live portal and runtime, which this task may not restart); the basics-path spec is written in `portal/e2e-tests/tests/admin.spec.ts` and mirrors the bundle one.
Interfaces produced: runtime `spatalk/tenants/starter.py`: `TenantBasics` (pydantic: `id` `^[a-z0-9-]{2,40}$`, `name` 2–80, `timezone` checked with `zoneinfo`, `hours: dict[str, list[tuple[str, str]]]` normalised to all seven days with at least one open, `booking_url: HttpUrl`, `public_phone` E.164 or "", `owner_name` default "", `owner_email` regex-checked and lowercased, `assistant_name` default "Ava"; helpers `staff_sms_env()` -> `<ID_UPPER>_STAFF_SMS`, `escalation_owner_name()`), `render_starter(basics) -> dict[str, str]` (the five filename→text pairs), `tenant_document(basics) -> dict`, `hours_in_words(hours)`, constants `STARTER`, `FIXED_FILES`, `SMS_GUARD`, `SOCIAL`, `DEFAULT_ASSISTANT_NAME`, `DIGEST_TIME_LOCAL`; package data `spatalk/tenants/starter/{scripts.yaml,guard.yaml,services.yaml,knowledge.md}`; `spatalk/http/internal.py`: `TenantBasicsIn(TenantBasics)` with `created_by: str = "portal"`, `POST /internal/tenants/from-basics` -> `TenantCreated`, 409 when the id exists, 422 when the basics do not make a tenant, audit `config_save`; `spatalk/brain/renderer.py`: `_values` gains `assistant_name`; `spatalk/cli.py`: `spatalk tenant new <id> --name --timezone --owner-email --booking-url [--owner-name] [--public-phone] [--assistant-name] [--hours-json] [--out DIR]`, `DEFAULT_HOURS`; contract path `/internal/tenants/from-basics` and schema `TenantBasicsIn` in `docs/contracts/runtime-internal.openapi.json` and `portal/src/runtime/client.ts`. Portal `src/admin/basics.ts`: `WEEKDAYS`, `Weekday`, `DayHours`, `BasicsDraft`, `RuntimeHours`, `DEFAULT_TIMEZONE`, `DEFAULT_ASSISTANT_NAME`, `OTHER_TIMEZONE`, `CANADIAN_TIMEZONES`, `isKnownTimezone(zone)`, `defaultBasics()`, `basicsProblems(draft): string[]`, `runtimeHours(draft)`; `src/admin/operations.ts`: `createTenantFromBasics({ name, slug, ownerEmail, ownerName, basics: { timezone, hours, bookingUrl, publicPhone, assistantName } }) -> NewTenant` (action, entities Organization, Membership, Invitation, declared in `admin.wasp.ts`), `BasicsArgs`, and the shared tail `organisationAndInvitation` / `addressTaken` behind both creators; `NewTenantWizard` step 2 "Configuration" with `wizard-mode-basics` / `wizard-mode-bundle` (`role="radio"`, `aria-checked`), the basics form test ids `basics-form`, `basics-timezone`, `basics-timezone-other`, `basics-<day>`, `basics-<day>-open`, `basics-<day>-start`, `basics-<day>-end`, `basics-booking-url`, `basics-public-phone`, `basics-assistant-name`, and `input[name="ownerName"]` on step 3 (basics only). `src/client/settings/NumbersTab.tsx`: the `ForwardLine` card, test ids `forward-line`, `forward-line-number`, `forward-line-copy`, `forward-line-unassigned`. `src/client/firstRun.ts`: `FIRST_RUN_KEYS`, `FirstRunKey`, `FirstRunStep`, `FirstRunConfig`, `FirstRunFacts`, `KNOWLEDGE_MIN_CHARS` (1000), `firstRunSteps(facts): FirstRunStep[]`, `firstRunDone(facts)`; `src/client/FirstRunCard.tsx`: `FirstRunCard({ steps })`, test ids `first-run-card`, `first-run-progress`, `first-run-step-<key>`, `first-run-done-<key>`; `src/client/operations.ts`: `getFirstRunChecklist({ slug }) -> { done, steps }` (query, `orgEntities`, declared in `pages.wasp.ts`), `FirstRunChecklist`.
Deviations:
- A: the starter's `scripts.yaml` is not a byte copy of Skincentrix's. The brief took the file for generic; nine of its lines name the clinic ("Skincentrix" in `followup`, `missed_call_text`, `offline_reply`, `optout_confirm`, `help_text`, `takeover_notice`, `dm_greeting`) or the assistant ("I'm Ava" in `disclosure` and `chat_greeting`). Those became `{name}` and `{assistant_name}`; every other byte is the same, and the Skincentrix bundle itself is untouched. Evidence: `test_the_starter_scripts_keep_the_runtime_placeholders_and_name_no_clinic` asserts no "skincentrix" in the starter and the placeholders present; the rendered file is still byte-identical to the starter (`test_the_rendered_scripts_and_guard_are_the_starter_files_byte_for_byte`).
- A: `{assistant_name}` did not exist as a placeholder, so `spatalk/brain/renderer.py` (`_values`) gained one from `cfg.persona.assistant_name`; a file outside the brief's list, changed because the starter scripts must reach a tenant unformatted and the persona's name would otherwise have been frozen as "Ava" for every clinic. `disclosure` and `chat_greeting` are only ever rendered through `render_script` (`grep -rn "disclosure\|chat_greeting" spatalk` -> `voice/pipeline.py:367`, `text/chat.py:151`), which supplies every `_values` key, so no other script path can see the new key; `test_the_placeholders_render_for_the_new_tenant` renders both for an assistant called Mia. `docs/reference/tenant-config.md` lists the placeholder.
- A: the starter's `guard.yaml` drops Skincentrix's two "skinc club" entries (`health_context`, `payment`); the rest is the same.
- A: `timezone` has no default on `TenantBasics`, so the contract lists it as required (seen: the contract test failed with it in the optional set). A clinic is asked rather than assumed (non-negotiable 8); `spatalk tenant new` keeps the `--timezone America/Toronto` default.
- A: `--booking-url` is required on `spatalk tenant new`, though the brief bracketed it: `booking_url_default` is a required config field with no sensible default, and an empty one would render "book online: " into texts.
- A: `knowledge.md` is a generated header (the name, phone, hours in words and booking link) followed by the starter's fixed body, not a fixed file alone: the skeleton would otherwise state nothing true about the clinic. Generated with plain string joins, not `str.format`, and only over the knowledge text.
- A: no `pyproject.toml` change was needed: hatchling's `packages = ["spatalk"]` already ships every file under the package (`rates.json`, `static/widget.js` were already in the wheel). Evidence: `uv build --wheel` then `zipfile` listing -> `spatalk/tenants/starter/guard.yaml`, `knowledge.md`, `scripts.yaml`, `services.yaml`.
- A: `tests/test_contract_snapshot.py` gained `/internal/tenants/from-basics` in its required-path set (strengthening, not weakening). `spatalk openapi --out` writes platform newlines on Windows, so the regenerated contract was normalised to LF before committing (git's `text=auto eol=lf` would have done the same at commit).
- A: the 409 check is `registry.get(id)` before rendering; a concurrent pair of identical posts could both pass it and the second would become version 2. Acceptable for a form an admin clicks once; noted so nobody reads the 409 as a database constraint.
- B: no `*.server.test.ts` existed beside `src/admin/operations.ts`; `src/admin/operations.server.test.ts` was created on `src/runtime/api.server.test.ts`'s pattern (mocked `wasp/server`, a stubbed `fetch` answering for the runtime, fake `Organization` delegate, `createAndSendInvitation` mocked).
- B: the steps stay Organisation → Configuration → Owner → What is left (the brief allowed a reorder); the bundle spec depends on the order and it reads fine. The owner's name is an optional field on step 3, shown for the basics path only, and goes to `escalation.owner_name` (the invitation has no name).
- B: the basics path checks the slug before calling the runtime, not after as the bundle path does: its tenant id is the slug, so a taken address would otherwise leave an orphan tenant behind on every retry. Both refusals come from one helper (`addressTaken`); the bundle path's text is unchanged and the basics text drops the "is now configuration version n" sentence for "nothing has been created yet". Both are pinned by the server test.
- B: the runtime's 409 for an existing tenant is read off the response before `runtimeCall` sees it, because `runtimeCall` folds every status but 404 and 422 into a 502 sentence; the admin gets "<id> already exists in the front desk service, so nothing was created" instead.
- B: `wasp compile` (Node 24.20.0 in WSL, as the branding report found) was needed to regenerate the SDK for the new action; the running dev server had not. It ran twice: the SDK build type-checks the test files too, and caught the server test's hours literal typed as `string[][]` rather than `[string, string][]` (`BasicsArgs` annotation fixed it). Wasp's install drifted `portal/package-lock.json`; restored with `git checkout`, not committed.
- C: `IconPhoneForwarded` does not exist in `@tabler/icons-react` (`ls node_modules/@tabler/icons-react/dist/esm/icons | grep Phone`); `IconPhoneIncoming` is the card's icon.
- D: the card is a separate component file, `src/client/FirstRunCard.tsx`, beside the pure `firstRun.ts` the brief named, with its own render test; `firstRun.ts` stays free of React so `getFirstRunChecklist` (server code) can import it.
- D: `getFirstRunChecklist` is its own query rather than a field of `getTenantOverview`, so a slow or failed read never holds up the numbers; open without a subscription, like the overview. Its facts come from `GET .../config`, the tenant summary's `numbers`, one row of `.../conversations` (`total > 0`) and one of `.../items?state=all` (`length > 0`).
- D: "knowledge or FAQ filled" is `KNOWLEDGE_MIN_CHARS` (1000) characters of knowledge or one FAQ entry: the starter skeleton is about 600 characters of headings and generic lines, so it does not tick the step (`firstRun.test.ts` pins that). "A staff destination" is an `email` with `address` or `address_env`, or an `sms` with `address_env`; Slack alone does not count.
- D: the card also shows "n of 8 done" (`first-run-progress`), not in the brief; three lines of markup.
Notes for neighbours:
- The runtime must be restarted for `POST /internal/tenants/from-basics` to be live (the orchestrator does that); the tests ran against the app in-process. `spatalk tenant new` needs no database or restart.
- The portal must be restarted for `createTenantFromBasics` and `getFirstRunChecklist` to be live; the SDK is already regenerated (`wasp compile`), so a restart is all it needs. No Prisma change, no migration.
- The wizard's basics path uses the organisation's slug as the runtime tenant id, so `/app/<slug>` and the tenant agree; the bundle path still takes the id from `tenant.yaml`.
- `{assistant_name}` is now a placeholder every tenant's scripts may use; Skincentrix's scripts do not use it and are unchanged.
- The starter bundle lives in `spatalk/tenants/starter/`, not `runtime/tenants/`, so `test_qa_gate_c`'s bundle glob and the e2e seed do not see it. A change to the fixed wording for new clinics is a change to that directory; Skincentrix keeps its own.
- The forwarding card states only the two conditions and the TELUS caller-id setting from the runbook; no carrier star codes or menu paths, because none in the repo could be verified.
- The checklist reads the configuration the runtime serves; a tenant configured by hand (an organisation whose runtime tenant does not exist) gets no card because the query fails and the overview shows its own problem as before.
