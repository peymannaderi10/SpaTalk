# onboarding-roadmap Task 4: the Playwright acceptance run
Status: done with deviations
Commit: 7b0b8a2 (five specs and the config follow the shell), fed8608 (the switcher case waits out the menu's exit animation; the basics case takes a per-run slug) (the switcher case waits out the menu's exit animation)
Tests: `npx playwright test` in WSL against an isolated stack -> first run 59 passed, 6 failed, 26 did not run (serial blocks stop at a failure); after the fixes, the six affected files -> 42 passed, 2 failed, 5 did not run; final full run -> 83 passed, 1 failed, 7 did not run (below); then `admin.spec.ts` + `qa-gate-b.spec.ts` -> 12 passed with one derived expectation left to fix, and `admin.spec.ts` alone -> 15/15; every spec file has passed in full across these runs. The full run after those fixes -> 83 passed, 1 failed, 7 did not run: the new basics-path wizard case reused a fixed slug, and the basics endpoint refuses a tenant that already exists (by design), so the case passed on its first run and failed on every run after; it now takes a per-run suffix like the switcher spec. Every failure was a stale expectation or the environment; no product defect was found, and the one candidate (the organisation switcher) was cleared by a throwaway diagnostic spec that drove the real menu through click, Escape, click, Enter and a raw mouse down/up (all opened and closed as designed; the spec was deleted, never committed).
Interfaces produced: `organisationForTenant(page, {name, slug, runtimeTenantId}) -> {id, slug}` in `portal/e2e-tests/tests/utils.ts`; `playwright.config.ts` webServer env pins `WASP_WEB_CLIENT_URL=http://localhost:3000` and `WASP_SERVER_URL` to `SERVER_URL`.
Deviations:
- The run is not "on the production build" as the roadmap gate says: it ran against the development server the suite starts itself (`wasp start`), because the production images are not deployed anywhere yet. The gate's production half stays open until the VPS exists; everything the suite can prove about the code, it proved.
- Isolated stack, not the laptop's dev data: the seed is destructive for the `skincentrix` tenant, and the founder's demo conversations live in the dev database. So a second runtime ran on `:8001` against `spatalk_test` with `SPATALK_NO_ENV_FILE=1` and only dummy provider ids (Instagram, Facebook and Slack app ids so the Connect cases build their URLs; no Telnyx key, no staff number, so nothing could be sent), bridged to WSL on `:8011` through a socat container (`spatalk-runtime-bridge`, because WSL cannot reach a Windows port), the seed pointed at that database through `RUNTIME_SEED_COMMAND` (a `.cmd` that sets `DATABASE_URL` before `seed_runtime.py`), and the portal at a fresh `portal_e2e` database migrated with `prisma migrate deploy`. The dev portal was stopped for each run (port 3000) and started again after.
- `admin.spec.ts` bundle case: the wizard's second step defaults to "Start from the basics" since 7f6e375, so the case selects `wizard-mode-bundle` first; the textareas it fills only exist in that mode. Evidence: `page.fill: waiting for locator('textarea[name="bundle-tenant"]')` for the full timeout.
- `client.spec.ts` and `shell.spec.ts` hours cases: the seed imports the real Skincentrix bundle, whose Monday has been closed since 87320c6; the cases now use Thursday, which opens at noon like the old Monday did, so the `12:00` expectations hold unchanged. Evidence: `getByTestId('hours-mon-0-start')` -> `<element(s) not found>`.
- `orgs.spec.ts` invitation case: the invitation link and every email carry `WASP_WEB_CLIENT_URL`, which the founder's `.env.server` points at a Cloudflare tunnel; the invite page opened on that origin parked its token in that origin's storage, and the sign-up round trip on `localhost:3000` never found it. Evidence: 24 of 29 links in `mail-sink.log` on the tunnel host; the page landed on `/app` and stayed. The webServer env now pins both Wasp URLs to localhost, so the suite is hermetic whatever `.env.server` says.
- `shell.spec.ts` and `sms-blocks.spec.ts` set-up: `Organization.runtimeTenantId` is unique, so a second organisation for the seeded tenant is refused with 409 and the specs' "409 means mine already exists" branch found nothing. `organisationForTenant` creates or adopts, matching by slug or by tenant, and both specs build their URLs from the slug that comes back (`let ORG_SLUG`). Evidence: `TypeError: Cannot read properties of undefined (reading 'id')` at the `find`.
- `shell.spec.ts` keyboard case: thirty tabs wrap round a sidebar that has one Settings entry instead of nine Setup ones, so the recorded order repeated; the case stops at the first repeated test id. Evidence: the received list held the six items twice.
- `qa-gate-b.spec.ts` switcher case: the previous case presses Escape and the next clicks the trigger at once; Radix keeps the closing menu mounted for its exit animation and the departing layer takes the pointer-down, so the menu ends closed. The case now waits for `role=menu` to reach count 0 after Escape. Evidence: trace snapshots after the second click show `data-state="closed"` and no menu; the diagnostic spec with 300 ms between steps opened and closed the menu six times in a row.
- `admin.spec.ts` basics case (found only on the final full run, which was the second run of that case): `basics-portal-e2e` already existed in the e2e runtime from the previous run and `POST /internal/tenants/from-basics` answered 409, so `wizard-result` never appeared. Evidence: the e2e runtime's tenant list held `basics-portal-e2e`; the bundle case survives re-runs because a re-uploaded bundle is a new version, the basics path never overwrites. The case now uses `basics-portal-e2e-<suffix>`.
- Not done: the e2e `tsconfig.json` still targets ES5, which TypeScript 6 reports as deprecated (TS5107); it does not affect Playwright's own transpiler and was left alone.
Notes for neighbours:
- To run the suite on this laptop: stop the dev portal inside WSL (`MSYS_NO_PATHCONV=1 wsl.exe -e bash /mnt/c/.../stop-portal.sh`), start the isolated runtime and bridge, then `npx playwright test` in WSL with `DATABASE_URL`, `RUNTIME_INTERNAL_URL=http://localhost:8011`, `RUNTIME_INTERNAL_KEY`, `RUNTIME_DATABASE_URL` and `RUNTIME_SEED_COMMAND` set; start the dev portal again after. On Linux (the VPS or CI) the README's one-runtime recipe is enough.
- A `pkill -f <pattern>` inside a `bash -c` string kills that shell too (its own command line matches); the stop script uses `[b]racket` patterns for that reason.
- The suite cannot pass with the Meta or Slack Connect cases unless the runtime has app ids for those providers; any value works, nothing is sent.
