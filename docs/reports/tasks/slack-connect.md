# slack-connect: a clinic connects its own Slack workspace with one click
Status: done with deviations
Commit: 4e8bf25 (A, runtime: settings, `tenant_integrations` columns and migration 0014, `spatalk/social/slack_oauth.py`, `spatalk/http/slack_connect.py`, the `slack` provider on `/internal/tenants/{id}/integrations`, delivery and the thread through the stored token, contract and client regenerated, runbooks); f5840f6 (B, portal: the Slack card on Settings → Integrations, `providerArgs` gains `slack`, a connected workspace ticks the checklist's staff-destination step)
Tests: runtime `uv run pytest -q -p no:cacheprovider tests/test_slack_connect.py tests/test_delivery.py tests/test_social_integrations_api.py -x` -> stopped at the first test on `ModuleNotFoundError: No module named 'spatalk.social.slack_oauth'` before any implementation (every other new test imports that module, calls `store_integration(channel_id=...)`, or hits `/slack/connect`, none of which existed); after the implementation 50/51, the one failure my own over-broad assertion (`"webhook" not in response.text` tripped on the scope word `incoming-webhook`; tightened to the URL key and host, which was the guarantee meant); then `... tests/test_contract_snapshot.py` -> 56/56; full suite `uv run pytest -q` -> 1325 passed, 2 skipped in 6 min 41 s (was 1297 + 2; the 28 new are 18 in `test_slack_connect.py`, 8 in `test_delivery.py`, 2 in `test_social_integrations_api.py`; the two skips are the live-key tests); `uv run ruff check spatalk tests scenarios` -> clean; `uv run alembic upgrade head` -> `0013 -> 0014` on the dev database, `uv run alembic check` -> "No new upgrade operations detected." Portal: `npx vitest run src/client/settings/Integrations.test.tsx src/client/firstRun.test.ts` -> 8 failed / 9 passed first (the seven card tests on the missing card, the checklist case on `[]` vs `['delivery']`), then with `src/client/FirstRunCard.test.tsx` -> 21/21; `npx tsc --noEmit -p tsconfig.src.json` -> two errors until `wasp compile` regenerated the SDK (`Type '"slack"' is not assignable to type '"instagram" | "messenger"'`), then clean. Playwright not run (needs a live portal and runtime, which this task may not restart); the Slack cases are written in `portal/e2e-tests/tests/integrations.spec.ts` and mirror the Instagram ones.
Interfaces produced: runtime `spatalk.settings.Settings.slack_client_id`, `.slack_client_secret`; `spatalk.social.models.TenantIntegration.channel_id: str | None` (VARCHAR 32), `.webhook_url_enc: str | None` (Fernet ciphertext), migration `alembic/versions/0014_slack_integration.py` (revision `0014`); `spatalk.social.meta_oauth.store_integration(..., channel_id: str | None = None, webhook_url: str | None = None)`; `spatalk.social.slack_oauth`: `PROVIDER = "slack"`, `SLACK_AUTHORIZE_URL`, `SLACK_API_BASE`, `SLACK_SCOPES`, `slack_redirect_uri(settings)`, `build_slack_start_url(settings, state)`, `SlackInstall`, `exchange_code(settings, code, client=None) -> SlackInstall`, `display_name(install)`, `complete_slack_connect(sf, settings, clock, *, code, state, connected_by, client=None) -> ConnectResult`, `slack_bot_token(integration, settings=None) -> str`, `slack_webhook_url(integration, settings=None) -> str | None`, `bot_token_for_tenant(sf, settings, tenant_id) -> str | None`, `revoke_integration(settings, integration, client=None) -> bool`; `spatalk.http.slack_connect.router` (`GET /slack/connect?tenant=&return_to=`, `GET /slack/callback?code&state`), `CONNECTED_BY = "slack connect link"`; `spatalk.http.internal.INTEGRATION_PROVIDERS = ("instagram", "messenger", "slack")` with `SOCIAL_PROVIDERS` kept as an alias, `_provider_configured(settings, "slack")`, `DELETE .../integrations/slack` revoking first; `spatalk.ledger.delivery`: `DeliveryPort.send_slack(webhook_url, blocks, text, token=None)`, `SlackBotDelivery.client_for(token)`, `.send_slack/.post_thread_root/.post_in_thread(..., token=None)`, `make_delivery` returning `SlackBotDelivery` when a global token or the client id and secret are set, `connected_slack_workspace(sf, tenant_id)`, the `deliver.slack` payload key `integration: true`, `MemoryDelivery.slack_tokens`, `MemoryBotDelivery.root_tokens` and `.thread_tokens`; `spatalk.text.takeover.slack_token_for(ctx, conversation_id) -> str | None`. Portal: `providerArgs` z.enum `["instagram", "messenger", "slack"]` (so `startIntegrationConnect` and `disconnectIntegration` accept `provider: "slack"`), `FirstRunFacts.slackConnected: boolean`, `getFirstRunChecklist` reading `GET /internal/tenants/{id}/integrations`, test ids `integration-slack`, `integration-slack-status`, `integration-slack-connect`, `integration-slack-disconnect`, `integration-slack-invite`, `integration-slack-unconfigured`.
Deviations:
- A: the enqueue loop is `schedule_item_delivery`, not `deliver_item` as the brief named it; evidence: `grep -rn "def deliver_item" runtime/spatalk` -> nothing, `grep -n "def schedule_item_delivery" runtime/spatalk/ledger/delivery.py` -> the loop the brief describes. Changed there.
- A: the in-memory bot fake is the existing `MemoryBotDelivery`, not `MemorySlackBotDelivery`; kept the name every existing test uses, no alias added.
- A: `spatalk/text/takeover.py` changed, though the brief said the thread relay needs none. The relay direction (Slack event → conversation by channel and ts) is indeed untouched and proven by `test_a_staff_reply_in_a_per_tenant_thread_still_finds_its_conversation`. The other direction was not: a thread root opened with a clinic's own token was then mirrored (`mirror_to_thread`) and annotated (`_post_note`) with the global token, which cannot post in another workspace, so the customer's messages and the "NOT DELIVERED" note would have been lost with a warning. Both now resolve the tenant's token through `slack_token_for`; evidence: `test_thread_mirrors_and_notes_go_out_with_the_workspaces_own_token` asserts `ctx.delivery.thread_tokens == [BOT_TOKEN, BOT_TOKEN]` with no global token set. Local import inside the function, so `text` still does not import `social` at module level.
- A: the bundle path's thread branch now also requires `settings.slack_bot_token`, where before it only checked that the delivery object had `post_thread_root`. Those were the same condition until `make_delivery` started returning `SlackBotDelivery` for a configured app with no global token; without the guard a bundle destination carrying `channel_id` and no global token would have tried a thread with an empty token instead of the webhook it uses today. Every existing test that uses `MemoryBotDelivery` sets the token (`grep -n slack_bot_token tests/test_takeover.py tests/test_slack_events.py`), and `test_without_a_connected_workspace_the_env_destination_is_used_exactly_as_before` plus the "not set; skipping" test pin the no-row path.
- A: a `not_in_channel` fallback, not in the brief. An install with the `incoming-webhook` scope authorises the webhook for the chosen channel; whether it also adds the bot user to that channel I could not verify against Slack from here, and `chat.postMessage` answers `not_in_channel` when it has not. Rather than dead-letter the job, `_deliver_slack_to_workspace` posts the item through the row's webhook and logs "Invite the bot to the channel for threads"; evidence: `test_a_bot_not_invited_to_the_channel_falls_back_to_the_webhook_with_a_warning`. The runbook step, the deploy step and the card's `integration-slack-invite` line all say to `/invite @Front Desk`.
- A: the revoke is reported through the existing `unsubscribed` field of `IntegrationRemoved` (the brief: mirror the Meta report), with the field's docstring saying what it means for Slack, rather than a new field; `IntegrationOut` is unchanged and the contract diff is descriptions only (5 lines). `spatalk openapi --out` wrote CRLF again; normalised to LF before committing. `npm run gen:client` changed `portal/src/runtime/client.ts` by 32 comment lines and left `package-lock.json` alone.
- A: the migration was written by hand in the `0013_flow_slots.py` style with revision `0014` (autogenerate names its file by a random id and would not have produced `0014_slack_integration`), then checked the way autogenerate checks: `uv run alembic upgrade head` (0013 -> 0014 on the dev database) and `uv run alembic check` -> "No new upgrade operations detected."
- A: the exchange and the revoke go through the existing `GraphClient` seam (`HttpGraphClient("https://slack.com/api")`), not slack_sdk's `AsyncWebClient`, so the tests inject `ctx.graph` exactly as the Meta tests do and the fake records the form body; the bot token is sent as the `token` form field of `auth.revoke` (Slack documents it as the alternative to the header) so `revoke.data == {"token": BOT_TOKEN}` is assertable. `oauth.v2.access` refusals come back 200 with `ok: false`, which is a 400 naming Slack's error word; the client secret is never in a response.
- A: `tests/test_social_integrations_api.py::test_both_providers_are_listed_as_not_connected_before_anyone_connects` became `test_every_provider_is_listed...` with `slack` in its expected list, and `_settings()` there gained the Slack credentials so its `configured` assertion stays all-true; a new test pins `configured: false` without them. A strengthening: the list is what the portal draws.
- A: a cancelled install (Slack sends `error=access_denied` and no `code`) is a 400 "missing code", the same as Instagram's; without `return_to` the callback answers a plain "Connected: <workspace · #channel>" line, never the token.
- B: `FirstRunFacts.slackConnected` is a required field, not optional: the query always knows it and a caller should have to say. `src/client/FirstRunCard.test.tsx`'s facts helper (outside the brief's list) gained the one line.
- B: `getFirstRunChecklist` adds a fifth parallel runtime read; a failure of it fails the checklist like the other four (the endpoint 404s only for an unknown tenant, when the config read already has).
- B: `wasp compile` (Node 24.20.0 in WSL, as the two previous reports found) was needed for `tsc` to see the new enum; `package-lock.json` drifted and was restored with `git checkout`, not committed.
- B: the card's second line for Slack does not say "renewed automatically" (a Meta fact: a bot token is revoked or reinstalled, never refreshed) but that the workspace stays connected until disconnected; the invite line and `xl:grid-cols-3` for three cards are the only other additions.
- B: the e2e "Connect asks the runtime for a signed Slack authorisation url" case skips itself when `get-tenant-integrations` reports `slack` unconfigured on that runtime, since the founder's `.env` decides that and a 409 there would be the runtime being honest, not a portal defect.
Notes for neighbours:
- The runtime must be restarted: two new routes (`GET /slack/connect`, `GET /slack/callback`), two new settings, and `make_delivery` now returns the bot delivery whenever `SLACK_CLIENT_ID` and `SLACK_CLIENT_SECRET` are set. The portal must be restarted: `providerArgs` and `getFirstRunChecklist` changed; the SDK is already regenerated (`wasp compile`). No Prisma change, no `wasp db migrate-dev`.
- `alembic upgrade head` was applied to the dev database: 0013 -> 0014 adds `runtime.tenant_integrations.channel_id` (VARCHAR 32, null) and `webhook_url_enc` (TEXT, null). Production gets it from `scripts/deploy.sh`'s migrate step; the test database is rebuilt from the models by the fixtures and needed nothing.
- What the founder does at api.slack.com (runbook `accounts-and-env.md` section 8 has the steps and the manifest): Basic Information → copy Client ID and Client Secret into `SLACK_CLIENT_ID` and `SLACK_CLIENT_SECRET` (notes on their own lines; `META_TOKEN_ENCRYPTION_KEY` must be set, it encrypts the Slack token and webhook too); OAuth & Permissions → Redirect URLs must hold `https://api.spatalk.ca/slack/callback` (the manifest now carries it under `oauth_config.redirect_urls`); Manage Distribution → Activate Public Distribution, without which only the agency's own workspace can install the app and a clinic's Connect ends on Slack's "not authorized" page; after a clinic connects, `/invite @Front Desk` in the channel it chose, or items arrive through the webhook without threads.
- Skincentrix keeps `SKINCENTRIX_SLACK_WEBHOOK` until someone clicks Connect; from then on that line and `SLACK_BOT_TOKEN` are ignored for it and each item is posted once (`test_a_connected_workspace_replaces_the_env_slack_destination`). Disconnecting puts the env destination back in charge on the next item.
- `SOCIAL_PROVIDERS` still imports, as an alias of `INTEGRATION_PROVIDERS`; `IntegrationOut` never carries a token, the webhook URL or the channel id (`test_the_integrations_list_reports_the_workspace_without_the_token_or_the_webhook`).
- A Slack row's `token_expires_at` is null, so the Meta refresh job leaves it alone; a revoked token shows up as a failed `deliver.slack` job, not as `needs_reconnect`.
- Known edge, not handled: a conversation whose thread root was opened in the agency workspace before the clinic connected keeps its stored channel and ts, so later items in that same conversation try that thread with the clinic's token and dead-letter; a new conversation starts a thread in the clinic's channel.
