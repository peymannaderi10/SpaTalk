# lead-context Task L1: Fields, summary, prompt, delivery
Status: done with deviations
Commit: 69dd0bf
Tests: `SPATALK_NO_ENV_FILE=1 TEST_DATABASE_URL=postgresql+asyncpg://spatalk:spatalk@localhost:5434/spatalk_test_l1 .venv/Scripts/python.exe -m pytest -q tests/test_lead_context.py tests/test_structural_honesty.py` -> 28/28; full suite -> 1013 passed, 1 failed, 2 skipped of 1016 (the one failure is pre-existing and unrelated; see Deviations). `ruff check spatalk tests scenarios` clean. `alembic upgrade head` -> 0011, `downgrade -1`, `upgrade head` all clean on a scratch database; `alembic check` reports "No new upgrade operations detected".

Interfaces produced: `spatalk.tenants.schema.TeamMember(name, role="")`, `DEFAULT_CONCERNS`, `TenantConfig.team: list[TeamMember] = []`, `TenantConfig.concerns: list[str]`, `TenantConfig.practitioner_names() -> list[str]`; `ItemDraft.returning_client|practitioner|concern`; `spatalk.brain.requests.LeadContext` (mixed into `CaptureRequest` and `BookingLinkRequest`); `spatalk.ledger.summary.{TYPE_LABELS, SUMMARY_LABELS, CALLBACK_TYPES, type_label, preferred_text, service_name, summarize_item}`; `spatalk.ledger.items.{practitioner_for, concern_for}`; `spatalk.http.internal.{item_out, DERIVED_ITEM_FIELDS}` and `ItemOut.{summary, service_name, preferred_text, returning_client, practitioner, concern}`; alembic revision `0011`.

Deviations:
- **`preferred_text` follows the plan's table, not the plan's illustrative sentence.** The plan gives the table `"any day" | "Thursday" | "Thursday afternoon" | "mornings"`, but its example summary renders `date="any", part_of_day="afternoon"` as `"any day, afternoons"`. The two contradict each other; the table is the explicit specification of the function, so `(any, afternoon)` renders `"afternoons"` and the worked example in the test reads `"... Callback afternoons."` Evidence: plan L1 Interfaces block, `preferred_text(window)` bullet vs. the `summarize_item` example on the line above it.
- **The staff SMS keeps its type-label head and gains the summary as its second part** rather than replacing the label. The plan says "the second line becomes the summary (replacing the bare type label)", but `tests/test_sms_staff_delivery.py` pins both `text.startswith("Skincentrix front desk #7:")` and `"Callback requested" in text`, and the summary of a callback that names a service is `"Callback: Facial. ..."`, which contains neither. Replacing the label would have meant weakening an existing test. The SMS now reads `"<name> front desk #7: Callback requested via voice. <summary> Who: ... Due ... Reply ACK ... Transcript: ..."`, so the item is described by the summary and the existing pins hold. Evidence: `tests/test_sms_staff_delivery.py:102` and `:104`.
- **Two label maps, to match both of the plan's worked examples.** `TYPE_LABELS` (the long staff labels, moved from `delivery.py` into `summary.py` to avoid an import cycle and re-imported by `delivery.py`) is what `service_name` falls back to; `SUMMARY_LABELS` (short: "New booking", "Callback", …) opens the sentence. That is the only way to get both `"New booking: Mirapeel facial …"` and `"Callback: Callback requested. Callback any day."` from one rule. Where a type has no short label (every escalation), the long label opens the sentence and is not repeated after the colon, so an escalation reads `"CLINICAL question."` rather than `"CLINICAL question: CLINICAL question."`
- **`. Callback {preferred_text}` is added for `new_booking` and `callback` only** ("when the item is a booking or callback"). A reschedule keeps its `Preferred:` fact line in the email, Slack and digest bodies, which now also renders through `preferred_text`, so nothing lost the window.
- **`ItemOut.service_name` is the catalog name or `None`**, while `summary.service_name(item, cfg)` returns the type label when there is no service, as the plan specifies for that function. The plan types the field `str | None`, and the portal (L2) must show no Service row when there is none, so the field carries the raw fact and the fallback lives only inside the sentence.
- **`tests/test_structural_honesty.py` field list widened to nine**, as the plan directs, with a docstring saying why the set is still closed. That is the deliberate amendment, mirrored in `CLAUDE.md` non-negotiable 2.
- **Pre-existing suite failure, not caused by this task**: `tests/test_ops_latency.py::test_the_eval_bot_runs_the_production_pipeline_in_the_production_order`. It compares the `Pipeline([...])` element list in `runtime/spatalk/voice/pipeline.py` against `runtime/scenarios/voice/eval_bot.py` by parsing both files. Commit `1fc602b` ("feat(voice): instant fixed acknowledgement while the model thinks") added `FillerProcessor(session)` to the production pipeline and did not add it to the eval bot. Neither of those two files, nor the test, is touched by this task (`git diff HEAD -- runtime/spatalk/voice/pipeline.py runtime/scenarios/voice/eval_bot.py runtime/tests/test_ops_latency.py` is empty), so the test's inputs are the committed ones and the failure exists at `HEAD` as it did before. I did not fix it: `eval_bot.py` belongs to the voice plan and aligning the harness changes what the eval measures. Evidence: `AssertionError ... At index 4 diff: 'llm' != 'FillerProcessor(session)'; Right contains one more item: 'assistant_agg'`.

Notes for neighbours:
- **L2 (portal)**: `ItemOut` now carries `summary`, `service_name` (null when the item has no service or the catalog dropped it), `preferred_text` (never `"any any"`), `returning_client` (true returning / false new / null not asked), `practitioner` (a team name, `"any"`, or null) and `concern`. `docs/contracts/runtime-internal.openapi.json` is regenerated and committed; run `npm run --silent gen:client` in `portal/` before touching `RequestsPage.tsx`. `practitionerLabel` should map `"any"` to "No preference" — the runtime stores the literal string `"any"`, not null, when the caller was asked and had no preference. That distinction is the whole reason the summary can say "no practitioner preference" only when the question was actually put.
- The bundle changed (`runtime/tenants/skincentrix/tenant.yaml` gains `team`), so the orchestrator's bundle import is required before the new practitioner enum reaches a live call. `concerns` is left to the schema default and is not written into the bundle.
- `TYPE_LABELS` moved from `spatalk/ledger/delivery.py` to `spatalk/ledger/summary.py`; `delivery.py` re-imports it, so `from spatalk.ledger.delivery import TYPE_LABELS` still works.
- The prompt names no offer wording: it says "the clinic's new-client offers listed in the facts", and the `$50` credit and the free virtual consultation stay in `knowledge.md`. A tenant that ends an offer edits one file.
- Scratch databases `spatalk_test_l1` and `spatalk_test_l1_mig` were dropped at the end of the run. The dev database `spatalk` and the running runtime were not touched.
