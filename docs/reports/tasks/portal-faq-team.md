# portal-faq-team Tasks 1-3: FAQ rows, a Team page, and the category check
Status: done with deviations
Commit: 873936b (Task 1, Knowledge page FAQ rows); f62e384 (Task 2, Team page); Task 3 needed no commit
Tests: `npx vitest run src/client/settings/KnowledgeTab.test.tsx src/client/settings/schemaFields.test.ts` -> 22/22 (8 new in KnowledgeTab.test.tsx, 5 new in schemaFields.test.ts; all 13 seen failing first, 12 on missing controls or `invalidAt`, 1 on `maxLength` undefined); `npx vitest run src/client/settings/TeamTab.test.tsx src/client/nav.test.ts` -> 28/28 (11 new in TeamTab.test.tsx, 1 new in nav.test.ts; seen failing on "Failed to resolve import ./TeamTab" and on the Setup list missing Team); full client suite `npx vitest run` -> 202/209, the 7 failures all in `src/admin/pricing.test.ts` and pre-existing (see deviations); server suite `npm run test:unit` -> 108/108; `npx tsc --noEmit -p tsconfig.src.json` -> exit 0 after each task (the Wasp SDK is present in `node_modules/wasp`, and `--listFiles` shows the 110 `src/client` files including `KnowledgeTab.tsx` and `SettingsPage.tsx`, so the clean exit is a real check). Playwright (`shell.spec.ts`) was edited but not run: it needs a running portal and runtime, which this task may not start or restart.
Interfaces produced: `TeamTab(props: TabProps)` in `portal/src/client/settings/TeamTab.tsx`; `TabProps.errors?: FieldPathError[]`, `FieldPathError = { path: string[] }`, `invalidAt(errors, path: (string | number)[]): boolean`, `SchemaField.maxLength?: number` in `portal/src/client/settings/schemaFields.ts`; `SchemaInput` prop `invalid?: boolean` (sets `aria-invalid`) and passes `field.maxLength` to text controls; `SettingsPage.TABS` gains `"Team"` between Services and Knowledge, slug `team`, reached as `?tab=team`; `nav.ts` `SETTINGS_TABS` gains `{ label: "Team", tab: "team", icon: IconUsers }` with testId `nav-settings-team`.
Deviations:
- The brief said to copy the Services tab's per-row error pattern; there was none. No tab received field errors at all (`grep -rn "fieldError\|errors" portal/src/client/settings/*.tsx` -> no matches); `SettingsPage` only listed them above the form. I added the pattern: `TabProps.errors`, `invalidAt()` (an error marks the field it names, anything inside it, and the fields under a refused container), and `SchemaInput.invalid` -> `aria-invalid`, which the kit's `Input`, `Textarea`, `Checkbox` and `SelectTrigger` already paint destructive. `SettingsPage` now passes `fieldErrors` in `tabProps`, and the Services rows use it too, so the brief's description is true from now on. Hours, Delivery and Scripts do not yet mark their controls; they receive `errors` and can.
- The brief said to use `ContentSection.tsx` the way the other tabs do; no tab uses it. It is the page frame `SettingsPage` renders once, and it owns the scroll container (`faded-bottom h-full overflow-y-auto`), so nesting it inside a tab would put a second scroll region inside the first. The FAQ section uses the tabs' own sub-section idiom instead (DeliveryTab's `<section>` with an `h3` and a muted line), with the helper text the brief gave word for word.
- Length limits come from the schema, but one level down from where the brief pointed: instead of the Knowledge tab reading `definition(schema, "FaqItem").properties.answer.maxLength` itself, `schemaFields.fieldFrom` now reads pydantic's `maxLength` (through `anyOf` for optional strings too) into `SchemaField.maxLength`, and `SchemaInput` passes it to the control. Same source, one place, and the Team page's Name gets its 80 from `TeamMember.name` the same way. Evidence: `uv run python -c "...TenantConfig.model_json_schema()..."` -> `FaqItem.answer.maxLength = 600`, `FaqItem.question.maxLength = 200`, `TeamMember.name.maxLength = 80`; `KnowledgeTab.test.tsx` "takes the length limits from the schema's FaqItem" asserts `maxlength="600"` and `"200"` on the rendered controls.
- The FAQ section renders only when the runtime's schema serves `FaqItem`. Against an older runtime the section is absent and `config.faq`, if any, is sent back untouched on save, since a save sends the whole draft.
- `portal/e2e-tests/tests/shell.spec.ts` pins the sidebar order in `SIDEBAR_ORDER`; `nav-settings-team` was added after `nav-settings-services`. That file was folded into the Task 2 commit by one `--amend` before anything was pushed, to keep one commit per task. Not run (see Tests).
- Task 3: nothing to change. `category` is already an editable text field on the Services page. Evidence: `grep -rn category portal/src --include=*.ts --include=*.tsx` (outside tests and the new TeamTab) -> no matches, so nothing filters it; `ServicesTab` renders `fieldsOf(schema, "Service")` unfiltered; the served schema has `category: { title: "Category", type: "string" }`, which `SchemaInput` draws as a text input. The helper line the brief offered was conditional on a fix, so none was added.
- The 7 failing tests in `src/admin/pricing.test.ts` are not from this work: `b1fbf80 chore(rates): the live stack prices Gemini 3.1 Flash-Lite` changed `docs/research/rates.json` after `6cbdcc1` pinned the quote figures. Evidence: `AssertionError: expected 'gemini_31_flash_lite' to be 'gemini_25_flash_lite'`; none of this task's files are imported by that test.
Notes for neighbours:
- The portal must be restarted for the Team tab and the FAQ rows to appear; `wasp start` does not recompile edits. Nothing here changes the runtime, the schema, or any operation: `saveTenantConfig` already sends the whole config as `z.record(z.string(), z.unknown())`.
- A tab now lives in three lists that the tests hold together: `SettingsPage.TABS`, `nav.ts SETTINGS_TABS` (pinned in order by `nav.test.ts`), and `shell.spec.ts SIDEBAR_ORDER`. Add to all three.
- `team[].services` stores `services[].id`. A service row that has no id yet is not offered as a tick box until it is saved with one, and a member with no ticks means every service, which the page says under each row.
- Test ids: `faq-<i>-question`, `faq-<i>-answer`, `add-faq`, `remove-faq-<i>`, `team-<i>-name`, `team-<i>-role`, `team-<i>-service-<service_id>`, `add-team-member`, `remove-team-<i>`, `team-empty`, `team-<i>-no-services`.
- A refused save's `FieldError.path` is already strings (`fieldErrorsFrom` maps `loc` through `String`), so `invalidAt(errors, ["faq", 2, "answer"])` matches `["faq", "2", "answer"]`; pass numbers or strings, either works.

# portal-faq-team Tasks 4-8: category bars, one Settings entry, the tenant mark, generated ids, capitalized categories
Status: done with deviations
Commit: 9b564d5 (Task 4, collapsible category bars with select-all); 4103e85 (Task 5, one Settings entry, in-page sub-nav); 57e8797 (Task 6, ids generated from the name, never shown); f07ee68 (Task 7, categories read capitalized, save lowercase); 5a225fc (Task 8, tenant mark and name, nothing under it)
Tests: Task 4 `npx vitest run src/client/settings/TeamTab.test.tsx` -> 15/15 (4 new, 5 rewritten for the bars; 9 seen failing first); Task 5 `npx vitest run src/client/nav.test.ts src/client/layout/AppLayout.test.tsx` -> 26/26 (2 new, 1 rewritten; 3 seen failing first); Task 6 `npx vitest run src/client/settings/ServicesTab.test.tsx src/client/settings/catalog.test.ts` -> 14/14 (all new; 6 of the tab's 7 seen failing first, the seventh — a saved id survives a rename — passed already and stays as the guard); Task 7 `npx vitest run src/client/settings/ServicesTab.test.tsx src/client/settings/TeamTab.test.tsx src/client/settings/catalog.test.ts` -> 32/32 (4 new or rewritten, seen failing first); Task 8 `npx vitest run src/client/components/tenant-mark.test.tsx src/client/components/layout/org-switcher.test.tsx src/client/layout/AppLayout.test.tsx` -> 17/17 (9 new, seen failing first); full client suite `npx vitest run` after Task 7 -> 240/240 (the pricing failures reported above are gone from the tree); `npx tsc --noEmit -p tsconfig.src.json` -> exit 0 after each task. Playwright `shell.spec.ts` was rewritten for Task 5 but not run (needs a live portal and runtime).
Interfaces produced: `SETTINGS_TABS` (exported, `as const`, `{label, tab, icon}` in sub-nav order) and `SettingsTab` (the label union) in `portal/src/client/nav.ts`, which `SettingsPage` now builds its tabs, sub-nav and slugs from; the sidebar's one Setup item `{ label: "Settings", to: "/app/:orgSlug/settings", testId: "nav-settings" }`; `SettingsNav` marks the current section with `aria-current="page"`; `TenantMark({ name, logoUrl?, className? })` in `portal/src/client/components/tenant-mark.tsx` (test id `tenant-mark`; an `<img alt={name}>` when `logoUrl` is set, the upper-cased initial on `bg-primary` otherwise, a building icon for a name with no letter); `slugifyServiceId`, `uniqueServiceId`, `SERVICE_ID_MAX` (40) and `displayCategory` in `portal/src/client/settings/catalog.ts`; `SchemaInput` prop `onBlur?: () => void` on its text controls; the kit's `Checkbox` draws a minus for `checked="indeterminate"`.
Deviations:
- Task 4: the kit's `Checkbox` showed the same check for a mixed state as for a ticked one, which would have read "all ticked" on a bar with some ticked, so `components/ui/checkbox.tsx` now draws `IconMinus` when `checked === "indeterminate"`. Radix turns a click on a mixed box into `true`, so select-all on a partly ticked bar completes the category, which is what the tests assert. Bars use Radix `Collapsible` with no `forceMount`, so the individual boxes are not in the document until a bar is opened; the tests open bars through `team-<i>-category-<category>-toggle`. Bar test ids: `team-<i>-category-<category>-bar` (carries "n of m"), `-all` (select-all box), `-toggle`.
- Task 5: rather than keep two spellings of the sections, `SettingsPage` now imports `SETTINGS_TABS` from `nav.ts` and derives `Tab`, `tabSlug` and `tabFromSlug` from it; the page's own `TABS` and per-section icons are gone, and `nav.ts` took the page's icons (`IconBook2`, `IconQuote`, `IconPlugConnected`) so the sub-nav looks as it did. The Setup section keeps its title with the single Settings entry in it, because `nav.test.ts`, `AppLayout.test.tsx` and `shell.spec.ts` all pin the three section titles. `sidebar-nav.tsx`'s `isActive` needed no change: an item with no query is current on any query, so the Settings entry stays lit on every `?tab=`; only its comments changed. `nav.test.ts` no longer excepts `/app/:orgSlug/settings` from the one-item-per-route rule.
- Task 6: "new row" is tracked as component state in `ServicesTab` (`naming: number[]`, indexes of rows added on this page whose id still follows the name), not as a marker on the draft, because the whole draft is sent to the runtime and `Service` is a plain pydantic model. A row leaves the set on the name's blur (`SchemaInput.onBlur`) — clicking Save blurs the name first, so a saved row is never in it — and removing a row re-indexes the set. A saved service therefore keeps its id on rename even when the id happens to equal its slug. The id is not rendered at all, not even hidden: the draft carries it and nothing in the DOM does. `uniqueServiceId` shortens the base so `_n` fits inside 40; accents are dropped (`Café Peel` -> `cafe_peel`).
- Task 7: capitalization is display-only and touches the first character only (`displayCategory`), so a trailing space or an inner capital typed mid-word stays where it was; the draft is lowercased on every change. The Team bars' test ids keep the raw lowercase category; only the label is capitalized. This lands beside the runtime's `823bef4 fix(config): a service category is stored lowercase however it was typed`, so a config saved before this commit is lowercased on the runtime side anyway.
- Task 8: the trigger keeps `aria-label="Organisation"` because `e2e-tests/tests/qa-gate-b.spec.ts` selects it with `getByLabel("Organisation")` and says so in its header; a label is not rendered text, and `org-switcher.test.tsx` asserts both that no "Organisation" or "Switch organisation" text renders and that the label stays. The "Switch organisation" hint for a member of several organisations went with the subtext, per "nothing under it"; the chevron still says the button opens. `TenantMark` uses a plain `<img>` rather than the kit's Radix `Avatar`, whose image only mounts after it loads and so never renders in jsdom. The dropdown's "Organisations" heading inside the menu is unchanged. `src/organizations/OrgSwitcher.tsx` (the old NavBar's) still says "Organisation"; it is not the clinic shell and was left alone.
- Line endings: the Python patch scripts wrote CRLF into the working copy on Windows; prettier normalised every touched file to LF before its commit, and one leftover (`SettingsPage.tsx` after a last inline edit) was restored from the index, content identical. Nothing committed carries CRLF.
Notes for neighbours:
- The tab list now lives in one place, `nav.ts SETTINGS_TABS`; add a section there (label, slug, icon) and a description in `SettingsPage.SECTIONS`, and `shell.spec.ts` needs nothing unless the sidebar changes. Sidebar test id for settings: `nav-settings`; sub-nav ids stay `settings-tab-<slug>`, now with `aria-current="page"` on the open one.
- A Branding page can pass `logoUrl` to `OrgSwitcher` -> `TenantMark`; `OrgSwitcher` currently calls `TenantMark` with the name only, so that is the one line to extend (plus threading the URL through `OrgAppLayout`).
- `services[].id` is never shown; a new row's id follows its name until the name loses focus. A runtime that refuses an empty id will do so on save for a row whose name was never typed, and the refusal lands on `services -> <i> -> id`, which the page lists above the form but no control marks (there is none).
- Categories: store lowercase, show `displayCategory`; anything new that prints a category should do the same.
