# portal-faq-team Tasks 1-3: FAQ rows, a Team page, and the category check
Status: done with deviations
Commit: 873936b (Task 1, Knowledge page FAQ rows); f62e384 (Task 2, Team page); Task 3 needed no commit
Tests: `npx vitest run src/client/settings/KnowledgeTab.test.tsx src/client/settings/schemaFields.test.ts` -> 22/22 (8 new in KnowledgeTab.test.tsx, 5 new in schemaFields.test.ts; all 13 seen failing first, 12 on missing controls or `invalidAt`, 1 on `maxLength` undefined); `npx vitest run src/client/settings/TeamTab.test.tsx src/client/nav.test.ts` -> 28/28 (11 new in TeamTab.test.tsx, 1 new in nav.test.ts; seen failing on "Failed to resolve import ./TeamTab" and on the Setup list missing Team); full client suite `npx vitest run` -> 202/209, the 7 failures all in `src/admin/pricing.test.ts` and pre-existing (see deviations); server suite `npm run test:unit` -> 108/108; `npx tsc --noEmit -p tsconfig.src.json` -> exit 0 after each task (the Wasp SDK is present in `node_modules/wasp`, and `--listFiles` shows the 110 `src/client` files including `KnowledgeTab.tsx` and `SettingsPage.tsx`, so the clean exit is a real check). Playwright (`shell.spec.ts`) was edited but not run: it needs a running portal and runtime, which this task may not start or restart.
Interfaces produced: `TeamTab(props: TabProps)` in `portal/src/client/settings/TeamTab.tsx`; `TabProps.errors?: FieldPathError[]`, `FieldPathError = { path: string[] }`, `invalidAt(errors, path: (string | number)[]): boolean`, `SchemaField.maxLength?: number` in `portal/src/client/settings/schemaFields.ts`; `SchemaInput` prop `invalid?: boolean` (sets `aria-invalid`) and passes `field.maxLength` to text controls; `SettingsPage.TABS` gains `"Team"` between Services and Knowledge, slug `team`, reached as `?tab=team`; `nav.ts` `SETTINGS_TABS` gains `{ label: "Team", tab: "team", icon: IconUsers }` with testId `nav-settings-team`.
Deviations:
- The brief said to copy the Services tab's per-row error pattern; there was none. No tab received field errors at all (`grep -rn "fieldError\|errors" portal/src/client/settings/*.tsx` -> no matches); `SettingsPage` only listed them above the form. I added the pattern: `TabProps.errors`, `invalidAt()` (an error marks the field it names, anything inside it, and the fields under a refused container), and `SchemaInput.invalid` -> `aria-invalid`, which the kit's `Input`, `Textarea`, `Checkbox` and `SelectTrigger` already paint destructive. `SettingsPage` now passes `fieldErrors` in `tabProps`, and the Services rows use it too, so the brief's description is true from now on. Hours, Delivery and Scripts do not yet mark their controls; they receive `errors` and can.
- The brief said to use `ContentSection.tsx` the way the other tabs do; no tab uses it. It is the page frame `SettingsPage` renders once, and it owns the scroll container (`faded-bottom h-full overflow-y-auto`), so nesting it inside a tab would put a second scroll region inside the first. The FAQ section uses the tabs' own sub-section idiom instead (DeliveryTab's `<section>` with an `h3` and a muted line), with the helper text the brief gave word for word.
- Length limits come from the schema, but one level down from where the brief pointed: instead of the Knowledge tab reading `definition(schema, "FaqItem").properties.answer.maxLength` itself, `schemaFields.fieldFrom` now reads pydantic's `maxLength` (through `anyOf` for optional strings too) into `SchemaField.maxLength`, and `SchemaInput` passes it to the control. Same source, one place, and the Team page's Name gets its 80 from `TeamMember.name` the same way. Evidence: `uv run python -c "...TenantConfig.model_json_schema()..."` -> `FaqItem.answer.maxLength = 600`, `FaqItem.question.maxLength = 200`, `TeamMember.name.maxLength = 80`; `KnowledgeTab.test.tsx` "takes the length limits from the schema's FaqItem" asserts `maxlength="600"` and `"200"` on the rendered controls.
- The FAQ section renders only when the runtime's schema serves `FaqItem`. Against an older runtime the section is absent and `config.faq`, if any, is sent back untouched on save, since a save sends the whole draft.
- `portal/e2e-tests/tests/shell.spec.ts` pins the sidebar order in `SIDEBAR_ORDER`; `nav-settings-team` was added after `nav-settings-services`. That file was folded into the Task 2 commit by one `--amend` before anything was pushed, to keep one commit per task. Not run (see Tests).
- Task 3: nothing to change. `category` is already an editable text field on the Services page. Evidence: `grep -rn category portal/src --include=*.ts --include=*.tsx` (outside tests and the new TeamTab) -> no matches, so nothing filters it; `ServicesTab` renders `fieldsOf(schema, "Service")` unfiltered; the served schema has `category: { title: "Category", type: "string" }`, which `SchemaInput` draws as a text input. The helper line the brief offered was conditional on a fix, so none was added.
- The 7 failing tests in `src/admin/pricing.test.ts` are not from this work: `b1fbf80 chore(rates): the live stack prices Gemini 3.1 Flash-Lite` changed `docs/research/rates.json` after `6cbdcc1` pinned the quote figures. Evidence: `AssertionError: expected 'gemini_31_flash_lite' to be 'gemini_25_flash_lite'`; none of this task's files are imported by that test.
Notes for neighbours:
- The portal must be restarted for the Team tab and the FAQ rows to appear; `wasp start` does not recompile edits. Nothing here changes the runtime, the schema, or any operation: `saveTenantConfig` already sends the whole config as `z.record(z.string(), z.unknown())`.
- A tab now lives in three lists that the tests hold together: `SettingsPage.TABS`, `nav.ts SETTINGS_TABS` (pinned in order by `nav.test.ts`), and `shell.spec.ts SIDEBAR_ORDER`. Add to all three.
- `team[].services` stores `services[].id`. A service row that has no id yet is not offered as a tick box until it is saved with one, and a member with no ticks means every service, which the page says under each row.
- Test ids: `faq-<i>-question`, `faq-<i>-answer`, `add-faq`, `remove-faq-<i>`, `team-<i>-name`, `team-<i>-role`, `team-<i>-service-<service_id>`, `add-team-member`, `remove-team-<i>`, `team-empty`, `team-<i>-no-services`.
- A refused save's `FieldError.path` is already strings (`fieldErrorsFrom` maps `loc` through `String`), so `invalidAt(errors, ["faq", 2, "answer"])` matches `["faq", "2", "answer"]`; pass numbers or strings, either works.
